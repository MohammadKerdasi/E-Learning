export interface Course {
    id: string;
    title: string;
    name: string;
    duration: string;
    level: string;
    description: string;
    image: string;
  }
export interface BasicHome{
  text:string,
  par:string,
  btn:string,
  varwidth:string
}